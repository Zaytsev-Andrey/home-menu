spring:
  application:
    name: "${name}"

  datasource:
    url: jdbc:postgresql://postgres:5432/recipe_service_db
    username: postgres
    password: password

  data:
    redis:
      host: redis
      port: 6379